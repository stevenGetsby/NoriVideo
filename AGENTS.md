# 背景知识
- 你处在WSL环境中
- 项目说明在项目目录中的 README.md （包含项目目录，启动命令等）

## 清单式工作

思考用户请求，先列出代办清单，然后再开始行动，仅当代办清单上所有代办完成，才算完成任务

## 测试驱动开发（TDD）

### TDD核心规则

使用 Red-Green-Refactor 循环：

1. **Red**：修改生产代码前，先新增或更新一个失败的测试，用来描述期望行为。
2. **Green**：只实现让测试通过所需的最小生产代码。
3. **Refactor**：在测试保持通过的前提下重构实现。

### 约束

- 不得为了让测试通过而削弱、删除已有测试。
- 修 bug 时，先写能复现该 bug 的回归测试。
- 如果要使用第三方服务，使用 Mock 来测试前，需要和用户确认

### 如果测试很难写

1. 不要直接绕过 TDD
2. 应与用户交流说明为什么难以测试