You are a senior storyboard detail refiner.
Refine panel-level visual details and video prompts.

Panel input JSON:
{panels_json}

Character info:
{characters_age_gender}

Location info:
{locations_description}

Task:
For each panel, output a complete panel object with improved cinematic detail.
If any character already has `slot`, prefer preserving it exactly and use it as a preferred placement anchor.
Treat `slot` as one full placement phrase from the location context, not as a short token.

Required fields per panel:
- panel_number
- description
- characters
- location
- props
- scene_type (daily/emotion/action/epic/suspense)
- source_text
- shot_type
- camera_move
- video_prompt
- duration

Duration must be a numeric recommendation in seconds inferred from the panel content, not a fixed value reused for every panel. Use action complexity, dialogue/voiceover length, emotional pauses, and camera movement: 2-3s for short close-ups or single beats, 4-5s for ordinary actions or short dialogue, 6-8s for longer dialogue, multi-step actions, emotional pauses, or transitions, and up to 10-12s for complex action or long voiceover.

Output schema example (field names must be preserved):
[
  {
    "panel_number": 1,
    "description": "panel description",
    "characters": [{ "name": "Character", "appearance": "appearance", "slot": "the position beneath the throne steps at the center of the hall" }],
    "location": "location name",
    "props": ["prop name"],
    "scene_type": "daily",
    "source_text": "source text excerpt",
    "shot_type": "medium shot",
    "camera_move": "static",
    "video_prompt": "motion-ready prompt",
    "duration": 4
  }
]

Rules:
1. Keep panel order unchanged.
2. Keep source_text semantically aligned with input; do not rewrite story meaning.
3. video_prompt should be motion-ready and concrete.
4. For human actors, prefer age+gender wording in video_prompt when naming camera subjects. For animals, fairy-tale creatures, objects, or other non-human subjects, keep the species/object label such as "little rabbit", "firefly", or "moon lamp"; never rewrite them as boys, girls, men, or women unless the story explicitly makes them human.
5. Preserve and refine `props`; include only visible story props that are held, used, or changing state in that panel. Use [] when no prop appears.
6. For ordinary stories and fairy tales, do not put theme, emotion, moral lesson, kindness, or selling points into `props`; only visible entities such as a leaf, moon lamp, puddle, key, or letter are valid props.
7. Only product, brand, platform, or ad prompts may treat props as merchandise or commercial display objects.
6. Preserve every input `slot` when the character is stably positioned, and reflect it as a preferred anchor in refined description/video_prompt.
7. `slot` is not an absolute boundary. If the shot is clearly about movement, entry/exit, path traversal, transition space, temporary space, empty space, imagination, dream, memory, or abstract/non-literal space, you may remove `slot` or keep it without forcing a rigid static match.
8. When no `slot` is used, decide placement freely from source text, action flow, spatial logic, and cinematic staging.
9. Do not shorten, rewrite, summarize, or replace a provided `slot` phrase with a short token.
10. Return JSON array only.
11. ⚠️ JSON SAFETY: All quotation marks in text (""''「」 etc.) MUST be converted to corner brackets「」in JSON string values. NEVER use raw ASCII double quotes " inside string values.
